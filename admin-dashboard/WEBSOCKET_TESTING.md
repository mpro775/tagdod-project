# دليل اختبار WebSocket للإشعارات

## الاختبار السريع

### 1. فتح لوحة الإدارة

1. افتح المتصفح واذهب إلى: `https://admin.allawzi.net`
2. سجل دخول كأدمن
3. افتح Developer Tools (F12)
4. اذهب إلى Console

### 2. التحقق من الاتصال

**النتائج المتوقعة في Console:**

```
✅ Connected to notifications socket
✅ Authenticated with notifications socket: { userId: "...", ... }
```

**إذا رأيت أخطاء:**
- تحقق من `VITE_API_BASE_URL` في `.env`
- تحقق من أن Backend يعمل
- تحقق من Nginx logs

### 3. اختبار الإشعارات

#### اختبار 1: إنشاء طلب جديد

1. من تطبيق العميل أو API، أنشئ طلب جديد
2. **النتيجة المتوقعة:**
   - ظهور إشعار في الجرس (🔔) في Header
   - تحديث العدد غير المقروء
   - إمكانية الضغط على الإشعار

#### اختبار 2: استخدام كوبون

1. أنشئ طلب جديد مع كوبون
2. **النتيجة المتوقعة:**
   - إشعار "استخدام كوبون" يظهر للمدراء

#### اختبار 3: تقييم طلب

1. من حساب عميل، قيّم طلب مكتمل
2. **النتيجة المتوقعة:**
   - إشعار "تقييم طلب" يظهر للمدراء

#### اختبار 4: تذكرة دعم فني

1. من حساب عميل، أنشئ تذكرة دعم فني
2. **النتيجة المتوقعة:**
   - إشعار "تذكرة دعم فني جديدة" يظهر للمدراء
   - عند إرسال رسالة: إشعار "رسالة دعم فني جديدة"

#### اختبار 5: طلب خدمة

1. من حساب عميل، أنشئ طلب خدمة
2. **النتيجة المتوقعة:**
   - إشعار "طلب خدمة جديد" يظهر للمدراء
   - عند تقديم عرض: إشعار "عرض جديد من مهندس"

### 4. اختبار التنقل

1. اضغط على أي إشعار
2. **النتيجة المتوقعة:**
   - الانتقال للصفحة الصحيحة:
     - إشعارات الطلبات → `/orders/:orderId`
     - إشعارات الخدمات → `/services/:serviceId`
     - إشعارات الدعم → `/support/:ticketId`

### 5. اختبار تحديد كمقروء

1. اضغط على "تحديد الكل كمقروء"
2. **النتيجة المتوقعة:**
   - اختفاء العدد غير المقروء
   - تغيير لون الإشعارات (تصبح أفتح)

---

## اختبار متقدم

### استخدام Browser DevTools

1. **Network Tab:**
   - ابحث عن `notifications` في Network
   - يجب أن ترى WebSocket connection (Status: 101 Switching Protocols)

2. **Console Commands:**
   ```javascript
   // فحص حالة الاتصال
   // (يجب أن يكون موجوداً في window إذا أضفت debug mode)
   ```

### اختبار إعادة الاتصال

1. أوقف Backend مؤقتاً
2. **النتيجة المتوقعة:**
   - Console: "❌ Disconnected from notifications socket"
3. أعد تشغيل Backend
4. **النتيجة المتوقعة:**
   - إعادة الاتصال تلقائياً
   - Console: "✅ Connected to notifications socket"

---

## استكشاف الأخطاء

### المشكلة: لا يوجد اتصال

**الحل:**
1. تحقق من `VITE_API_BASE_URL` في `.env`
2. تحقق من Console للأخطاء
3. تحقق من Network tab في DevTools

### المشكلة: الإشعارات لا تظهر

**الحل:**
1. تحقق من أن المستخدم لديه دور `ADMIN` أو `SUPER_ADMIN`
2. تحقق من Backend logs
3. تحقق من Console للأخطاء

### المشكلة: التنقل لا يعمل

**الحل:**
1. تحقق من Routes في `routes.tsx`
2. تحقق من Console للأخطاء JavaScript
3. تحقق من أن `data.orderId` أو `data.ticketId` موجود في الإشعار

---

## ملاحظات

- الإشعارات تُرسل فقط للمدراء (ADMIN, SUPER_ADMIN)
- الاتصال يعيد المحاولة تلقائياً عند الانقطاع
- الإشعارات تُحفظ في قاعدة البيانات للرجوع إليها

