# نظام الدفع المحلي - Frontend Documentation

## 📋 نظرة عامة

تم تحديث نظام إدارة الحسابات المحلية في لوحة التحكم لدعم إدارة حسابات الدفع المحلية (البنوك والمحافظ) بشكل كامل.

## 🗂️ الملفات المعدلة/المضافة

### 1. API (`api/systemSettingsApi.ts`)

#### الأنواع المضافة:

```typescript
// حساب دفع محلي
export interface LocalPaymentAccount {
  _id: string;
  providerName: string;
  iconMediaId?: string;
  icon?: MediaReference;
  type: 'bank' | 'wallet';
  numberingMode: 'shared' | 'per_currency';
  supportedCurrencies: Array<'YER' | 'SAR' | 'USD'>;
  sharedAccountNumber?: string;
  accounts: Array<{
    id: string;
    currency: 'YER' | 'SAR' | 'USD';
    accountNumber: string;
    isActive: boolean;
    displayOrder: number;
    notes?: string;
    isOverride: boolean;
  }>;
  isActive: boolean;
  notes?: string;
  displayOrder: number;
  updatedBy?: string;
  createdAt?: string;
  updatedAt?: string;
}

// حساب مجمع (مجمعة حسب providerName)
export interface GroupedPaymentAccount {
  providerId: string;
  providerName: string;
  icon?: MediaReference;
  type: 'bank' | 'wallet';
  numberingMode: 'shared' | 'per_currency';
  supportedCurrencies: Array<'YER' | 'SAR' | 'USD'>;
  sharedAccountNumber?: string;
  accounts: Array<{
    id: string;
    accountNumber: string;
    currency: 'YER' | 'SAR' | 'USD';
    isActive: boolean;
    displayOrder: number;
    notes?: string;
  }>;
}
```

#### API Functions:

```typescript
localPaymentAccountsApi = {
  // جلب جميع الحسابات (مفصلة)
  getAllAccounts(activeOnly?: boolean): Promise<LocalPaymentAccount[]>

  // جلب الحسابات مجمعة حسب providerName
  getGroupedAccounts(activeOnly?: boolean): Promise<GroupedPaymentAccount[]>

  // جلب الحسابات العامة للعملاء
  getPublicAccounts(currency?: string): Promise<GroupedPaymentAccount[]>

  // إنشاء حساب جديد
  createAccount(data: CreatePaymentAccountDto): Promise<LocalPaymentAccount>

  // تحديث حساب موجود
  updateAccount(id: string, data: UpdatePaymentAccountDto): Promise<LocalPaymentAccount>

  // حذف حساب
  deleteAccount(id: string): Promise<void>
}
```

### 2. SystemSettingsPage (`pages/SystemSettingsPage.tsx`)

#### Tab جديد: "حسابات الدفع المحلية"

**الميزات:**
- جدول يعرض مزودي الحسابات (البنوك/المحافظ) مع الأيقونات والنوع ووضع الترقيم.
- عند وضع **مشترك** يتم عرض الرقم الموحد واللغات المدعومة والتخصيصات لكل عملة (إن وجدت).
- عند وضع **لكل عملة** يتم عرض كل حساب مستقل بالعملة ورقم الحساب والحالة.
- إجراءات التحرير والحذف أصبحت على مستوى المزوّد بالكامل.

**Dialog لإضافة/تعديل المزوّد:**
- اسم المزوّد والأيقونة (اختياري).
- اختيار النوع (بنك / محفظة) ووضع الترقيم (مشترك أو لكل عملة).
- في وضع المشترك: إدخال رقم الحساب الموحد وتحديد العملات المدعومة وإضافة التخصيصات.
- في وضع لكل عملة: إنشاء حسابات فرعية لكل عملة مطلوبة.
- لكل حساب فرعي يمكن ضبط رقم الحساب، الحالة، ترتيب العرض والملاحظات.
- إعدادات عامة: حالة التفعيل، ترتيب العرض العام، الملاحظات.

## 🎨 UI Components

### الجدول
```
┌────────────────────────────────────────────────────────────┐
│ اسم البنك    │ النوع  │ الحسابات          │ الإجراءات    │
├────────────────────────────────────────────────────────────┤
│ الكريمي 🏦  │ بنك    │ YER: 1234567890   │ ✏️ 🗑️       │
│              │        │ SAR: 9876543210   │              │
│              │        │ USD: 5555555555   │              │
└────────────────────────────────────────────────────────────┘
```

### Dialog الإضافة/التعديل
- نموذج منظم مع جميع الحقول
- Validation: اسم البنك ورقم الحساب مطلوبان
- Switch لتفعيل/تعطيل الحساب

## 🔄 State Management

```typescript
// State للبيانات
const [paymentAccounts, setPaymentAccounts] = useState<GroupedPaymentAccount[]>([]);
const [allAccounts, setAllAccounts] = useState<LocalPaymentAccount[]>([]);

// State للـ Dialog
const [accountDialogOpen, setAccountDialogOpen] = useState(false);
const [editingAccount, setEditingAccount] = useState<LocalPaymentAccount | null>(null);

// نموذج المزوّد (حقل واحد للحساب المشترك + حسابات فرعية اختيارية)
type PaymentProviderForm = {
  providerName: string;
  iconMediaId?: string | null;
  type: 'bank' | 'wallet';
  numberingMode: 'shared' | 'per_currency';
  sharedAccountNumber?: string;
  supportedCurrencies: Array<'YER' | 'SAR' | 'USD'>;
  accounts: ProviderAccountInput[];
  isActive: boolean;
  notes?: string;
  displayOrder: number;
};

const [accountForm, setAccountForm] = useState<PaymentProviderForm>(createDefaultForm());
```

## 📡 API Calls

### جلب الحسابات
```typescript
const fetchPaymentAccounts = async () => {
  const [grouped, all] = await Promise.all([
    localPaymentAccountsApi.getGroupedAccounts(),
    localPaymentAccountsApi.getAllAccounts(),
  ]);
  setPaymentAccounts(grouped);
  setAllAccounts(all);
};
```

### إنشاء/تحديث حساب
```typescript
const buildPayload = (): CreatePaymentAccountDto => ({
  providerName: accountForm.providerName.trim(),
  iconMediaId: accountForm.iconMediaId ?? null,
  type: accountForm.type,
  numberingMode: accountForm.numberingMode,
  sharedAccountNumber: accountForm.numberingMode === 'shared'
    ? accountForm.sharedAccountNumber?.trim()
    : undefined,
  supportedCurrencies: accountForm.numberingMode === 'shared'
    ? accountForm.supportedCurrencies
    : undefined,
  accounts: normalizeAccountsForPayload(),
  isActive: accountForm.isActive,
  notes: accountForm.notes,
  displayOrder: accountForm.displayOrder ?? 0,
});

const handleSaveAccount = async () => {
  const payload = buildPayload();

  if (editingAccount) {
    await localPaymentAccountsApi.updateAccount(editingAccount._id, payload);
  } else {
    await localPaymentAccountsApi.createAccount(payload);
  }

  fetchPaymentAccounts();
};
```

## 🎯 Use Cases

### Use Case 1: إضافة مزوّد بحساب مشترك
1. الانتقال إلى: الإعدادات > حسابات الدفع المحلية.
2. الضغط على "إضافة حساب".
3. تعبئة الحقول الأساسية (الاسم، النوع، الأيقونة).
4. اختيار وضع الترقيم **مشترك**، إدخال رقم الحساب الموحد وتحديد العملات المدعومة (مثلاً YER و SAR).
5. حفظ.

### Use Case 2: تخصيص حساب لعملة محددة (وضع مشترك)
1. فتح نافذة التحرير للمزوّد المشترك.
2. ضمن قسم "تخصيص حسابات العملات" إضافة صف جديد لعملة SAR.
3. إدخال رقم الحساب الخاص بتلك العملة (إذا كان مختلفًا).
4. حفظ التعديلات.

### Use Case 3: إنشاء مزوّد بحسابات منفصلة لكل عملة
1. إضافة مزوّد جديد واختيار وضع الترقيم **لكل عملة**.
2. إضافة صف لكل عملة مطلوبة (YER، SAR، USD) مع رقم الحساب الخاص بكل واحدة.
3. ضبط ترتيب العرض والحالة لكل حساب.
4. حفظ.

### Use Case 4: تعديل مزوّد موجود
1. الضغط على زر التحرير المقابل للمزوّد.
2. تحديث البيانات العامة أو الحسابات الفرعية (رقم حساب، ملاحظات، حالة...إلخ).
3. حفظ التغييرات.

### Use Case 5: حذف مزوّد
1. الضغط على زر الحذف بجانب المزوّد.
2. تأكيد العملية في مربع الحوار.
3. يتم حذف المزوّد وجميع حساباته الفرعية من قاعدة البيانات.

---

## 📊 Orders Module Updates

### 1. Types (`orders/types/order.types.ts`)

**الحقول المضافة إلى Order:**
```typescript
// Local Payment
localPaymentAccountId?: string;
paymentReference?: string;
verifiedPaymentAmount?: number;
verifiedPaymentCurrency?: string;
paymentVerifiedAt?: Date;
paymentVerifiedBy?: string;
paymentVerificationNotes?: string;
```

**Type جديد:**
```typescript
export interface VerifyPaymentDto {
  verifiedAmount: number;
  verifiedCurrency: 'YER' | 'SAR' | 'USD';
  notes?: string;
}
```

### 2. API (`orders/api/ordersApi.ts`)

**Function جديد:**
```typescript
verifyPayment(id: string, data: VerifyPaymentDto): Promise<Order>
```

### 3. Hooks (`orders/hooks/useOrders.ts`)

**Hook جديد:**
```typescript
export const useVerifyPayment = () => {
  // Mutation للمطابقة
  // رسالة نجاح/فشل بناءً على paymentStatus
  // تحديث تلقائي للبيانات
}
```

### 4. OrderDetailsPage (`orders/pages/OrderDetailsPage.tsx`)

#### قسم "معلومات الدفع المحلي"

**يظهر فقط إذا:**
- `paymentMethod === BANK_TRANSFER`
- `localPaymentAccountId` موجود

**يعرض:**
- رقم الحوالة/المرجع
- معلومات المطابقة (إذا تمت):
  - المبلغ المطابق
  - العملة المطابقة
  - تاريخ المطابقة
  - ملاحظات المطابقة
- حالة القبول/الرفض:
  - Alert أخضر: "تم قبول الدفع - المبلغ كافٍ"
  - Alert أحمر: "تم رفض الدفع - المبلغ غير كافٍ"
- Alert تحذيري: "في انتظار مراجعة ومطابقة الدفع"

#### زر "مطابقة الدفع"

**يظهر فقط إذا:**
- `paymentStatus === PENDING`

**عند الضغط:**
- يفتح Dialog المطابقة

#### Dialog المطابقة

**الحقول:**
1. **المبلغ المطابق** (number, required)
   - Input type: number
   - Step: 0.01
   - Min: 0

2. **العملة** (select, required)
   - Options: YER, SAR, USD

3. **ملاحظات** (text, optional)
   - Multiline
   - 3 rows

**الميزات:**
- يعرض المبلغ المطلوب في Alert
- تنبيه فوري أثناء الإدخال:
  - إذا المبلغ >= المطلوب → Alert أخضر: "المبلغ كافٍ - سيتم قبول الدفع"
  - إذا المبلغ < المطلوب → Alert أصفر: "المبلغ غير كافٍ - سيتم رفض الدفع (نقص: X)"
- زر "مطابقة" معطّل إذا لم يتم إدخال مبلغ

**عند الحفظ:**
- يتم استدعاء `verifyPayment` API
- يتم تحديث حالة الطلب تلقائياً
- يتم إغلاق Dialog
- يتم تحديث البيانات

## 🔄 Complete Flow Example

### Scenario: طلب جديد مع دفع محلي

1. **العميل يختار الدفع المحلي:**
   - يختار "الكريمي" > حساب YER
   - يدخل رقم الحوالة: "REF123456"

2. **الطلب يصل للوحة التحكم:**
   - الحالة: `PENDING_PAYMENT`
   - حالة الدفع: `PENDING`
   - يظهر قسم "معلومات الدفع المحلي"
   - يظهر زر "مطابقة الدفع"

3. **الإدارة تراجع:**
   - تفتح الطلب
   - ترى رقم الحوالة: "REF123456"
   - تفتح Dialog المطابقة

4. **الإدارة تطابق:**
   - تدخل المبلغ: 50000
   - العملة: YER
   - ملاحظات: "تم التحقق من الحوالة"

5. **النظام يتخذ القرار:**
   - يقارن: 50000 >= 45000 (مثال)
   - النتيجة: قبول
   - التحديثات:
     - `paymentStatus`: `PAID`
     - `status`: `CONFIRMED`
     - `verifiedPaymentAmount`: 50000
     - `paymentVerifiedAt`: الآن
     - Alert أخضر: "تم قبول الدفع بنجاح"

6. **النتيجة:**
   - الطلب أصبح `CONFIRMED`
   - الدفع `PAID`
   - جاهز للمعالجة

## 🎨 UI Screenshots (Description)

### صفحة إدارة الحسابات
```
┌─────────────────────────────────────────────────────┐
│  حسابات الدفع المحلية          [إضافة حساب]        │
├─────────────────────────────────────────────────────┤
│ الكريمي 🏦                                          │
│   نوع: بنك                                          │
│   الحسابات:                                         │
│   • YER: 1234567890        [✏️] [🗑️]              │
│   • SAR: 9876543210        [✏️] [🗑️]              │
│   • USD: 5555555555        [✏️] [🗑️]              │
└─────────────────────────────────────────────────────┘
```

### Dialog الإضافة/التعديل
```
┌────────────────────────────────────┐
│  إضافة حساب جديد                   │
├────────────────────────────────────┤
│  اسم البنك/المحفظة: [________]    │
│  رابط الأيقونة: [________]        │
│  النوع: [بنك ▼]                   │
│  رقم الحساب: [________]           │
│  العملة: [YER ▼]                  │
│  ترتيب العرض: [0]                 │
│  ملاحظات: [________]              │
│                                    │
│  ☑ الحساب مفعل                     │
│                                    │
│  [إلغاء]  [إضافة]                 │
└────────────────────────────────────┘
```

### صفحة تفاصيل الطلب - قسم الدفع المحلي
```
┌─────────────────────────────────────────────────┐
│  معلومات الدفع المحلي    [مطابقة الدفع]       │
├─────────────────────────────────────────────────┤
│  رقم الحوالة / المرجع: REF123456               │
│                                                 │
│  ⚠️ في انتظار مراجعة ومطابقة الدفع            │
└─────────────────────────────────────────────────┘
```

### Dialog المطابقة
```
┌────────────────────────────────────┐
│  مطابقة الدفع المحلي               │
├────────────────────────────────────┤
│  ℹ️ المبلغ المطلوب: 45,000 YER    │
│                                    │
│  المبلغ المطابق: [50000]          │
│  العملة: [YER ▼]                  │
│  ملاحظات: [________]              │
│                                    │
│  ✅ المبلغ كافٍ - سيتم قبول الدفع │
│                                    │
│  [إلغاء]  [مطابقة]                │
└────────────────────────────────────┘
```

## 🔧 Configuration

لا يحتاج إلى إعدادات إضافية. النظام يعمل مباشرة بعد التحديثات.

## 📝 Notes

1. **الحسابات المجمعة:** يتم الجمع على مستوى Backend، Frontend يعرض البيانات كما هي.

2. **التحديث التلقائي:** بعد أي عملية (إنشاء/تحديث/حذف)، يتم إعادة جلب البيانات تلقائياً.

3. **التحرير:** للتحرير، نحتاج الحساب الكامل (من `allAccounts`)، لذلك يتم جلب `grouped` و `all` معاً.

4. **الأمان:** جميع endpoints تتطلب Admin authentication.

---

**تاريخ الإنشاء:** 2024  
**آخر تحديث:** 2024

